package VeryEasy;

public class findPerimeter {
    public static int findPerimeterInForm(int length, int width) {
        int perimeter = (length * 2) + (width * 2);
        return perimeter;
    }
}
